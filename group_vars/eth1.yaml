packages:
  apt_repositories:
    beacon:
      - ppa:ethereum/ethereum
  apt:
    beacon:
      - ethereum

configuration:
  geth:
    path: /etc/geth.toml
    eth:
      networkid: 1
    node:
      datadir: "~/.ethereum"

